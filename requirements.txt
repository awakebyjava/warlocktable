# Warlock Table v2 — Python dependencies
#
# Laptop (dev):  pip install -r requirements.txt
# Raspberry Pi:  see deploy/README.md — do NOT use this file directly there,
#                because pixelblaze-client pulls in mini-racer, which has no
#                ARM wheel and tries to build V8 from source.

pixelblaze-client>=1.1.8

# QR code for the join page. Pure Python, no dependencies, so unlike most of
# the imaging stack it installs on the Pi without incident. Optional at
# runtime: warlock/qr.py degrades to printing the URL if it is absent.
segno>=1.5

# Pixels dice need NO dependency: the scanner talks raw HCI from the
# standard library (tools/dice_probe.py explains why bleak was dropped --
# BlueZ 5.55's discovery API delivers one update a minute on this Pi).
