// Boon-Wands -- GENERATED by tools/patterngen.py. Do not edit here.
// One comet lap, then the ring blooms. Ace of Wands.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
cWide = 26
cN = 1
cTail = 6
ct = 0
hA = 0.045
hB = 0.045
sA = 0.95
sB = 0.95
vA = 0
vB = 0.85
function cometField(pp) {
  acc = 0
  for (ci = 0; ci < cN; ci++) {
    hp = (ct + ci / cN) * pathLen
    dd = pp - hp
    dd = dd - pathLen * floor(dd / pathLen + 0.5)
    wide = cWide
    if (dd < 0) wide = cWide * cTail
    dd = abs(dd)
    if (dd < wide) {
      fl = 1 - dd / wide
      acc = acc + fl * fl
    }
  }
  return min(acc, 1)
}
export function beforeRender(delta) {
  dt = delta / 1000
  ct = time(0.27466) * 1
  et = time(0.27466)
  env = 0
  if (et < 0.22) {
    pk = (et / 0.22) * 1
    env = 1 - abs((pk - floor(pk)) * 2 - 1)
    env = env * env
  }
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = cometField(p)
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  v = v * env
  hsv(h, sa, v)
}
