// Aura-Devil -- GENERATED by tools/patterngen.py. Do not edit here.
// Standalone aura, played when no scene is running.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
hA = 0.02
hB = 0.02
sA = 0.95
sB = 0.95
vA = 0.04
vB = 0.6
env = 1
export function beforeRender(delta) {
  dt = delta / 1000
  env = env * 0.55 + (0.15 + 0.85 * random(1)) * 0.45
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = 1
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  v = v * env
  hsv(h, sa, v)
}
