// Aura-Temperance -- GENERATED by tools/patterngen.py. Do not edit here.
// Standalone aura, 9s. Arrives, plays, leaves.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
cueEl = 0
gate = 0
hA = 0.45
hB = 0.45
sA = 0.75
sB = 0.75
vA = 0.04
vB = 0.6
export function beforeRender(delta) {
  dt = delta / 1000
  cueEl = cueEl + dt
  env = 0.35 + 0.65 * wave(time(0.12207))
  gate = min(cueEl / 0.5, 1)
  gOut = (9 - cueEl) / 1.2
  if (gOut < 0) gOut = 0
  if (gOut > 1) gOut = 1
  gate = gate * gOut
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = 1
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  v = v * env * gate
  hsv(h, sa, v)
}
