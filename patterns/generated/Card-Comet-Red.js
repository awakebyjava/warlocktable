// Card-Comet-Red -- GENERATED by tools/patterngen.py. Do not edit here.
// One red comet lap. Number cards, red suits.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
halfLen = pathLen / 2
invPathLen = 1 / pathLen
cueEl = 0
gate = 0
cWide = 26
cN = 1
cTail = 6
ct = 0
hA = 0
hB = 0
sA = 0.95
sB = 0.95
vA = 0
vB = 0.85
function cometField(pp) {
  acc = 0
  for (ci = 0; ci < cN; ci++) {
    hp = (ct + ci / cN) * pathLen
    dd = pp - hp
    if (dd > halfLen) dd = dd - pathLen
    if (dd < -halfLen) dd = dd + pathLen
    wide = cWide
    if (dd < 0) wide = cWide * cTail
    dd = abs(dd)
    if (dd < wide) {
      fl = 1 - dd / wide
      acc = acc + fl * fl
    }
  }
  return min(acc, 1)
}
export function beforeRender(delta) {
  dt = delta / 1000
  cueEl = cueEl + dt
  ct = (cueEl / 2.5) * 1
  gate = min(cueEl / 0.12, 1)
  gOut = (2.9 - cueEl) / 0.45
  if (gOut < 0) gOut = 0
  if (gOut > 1) gOut = 1
  gate = gate * gOut
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p * invPathLen
  raw = cometField(p)
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  v = v * gate
  hsv(h, sa, v)
}
