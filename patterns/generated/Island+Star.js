// Island+Star -- GENERATED by tools/patterngen.py. Do not edit here.
// Island under the Star aura.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
wfA = 2
wsA = 0.1
wfB = 3
wsB = 0.07
hA = 0.55
hB = 0.5185
sA = 0.62
sB = 0.395
vA = 0.05
vB = 0.42
N = 9
bWide = 4
bGain = 0.9
bMin = 0.5
bVar = 1.2
bRest = 1.5
bHue = 0.55
bPos = array(N)
bAge = array(N)
bLife = array(N)
thr = 0.88
thrGain = 0.35
for (i = 0; i < N; i++) {
  bPos[i] = random(pathLen)
  bLife[i] = bMin + random(bVar)
  bAge[i] = random(bLife[i])
}
export function beforeRender(delta) {
  dt = delta / 1000
  tA = time(wsA)
  tB = time(wsB)
  env = 1
  for (i = 0; i < N; i++) {
    bAge[i] = bAge[i] + dt
    if (bAge[i] >= bLife[i]) {
      bAge[i] = -random(bRest)
      bLife[i] = bMin + random(bVar)
      bPos[i] = random(pathLen)
    }
  }
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = wave(u * wfA - tA) * 0.55 + wave(u * wfB + tB) * 0.45
  f = raw
  h = hA + (hB - hA) * (f * f * f)
  sa = sA + (sB - sA) * (f * f * f)
  v = vA + (vB - vA) * (f * f)
  b = 0
  for (j = 0; j < N; j++) {
    if (bAge[j] > 0) {
      dd = p - bPos[j]
      dd = dd - pathLen * floor(dd / pathLen + 0.5)
      dd = abs(dd)
      if (dd < bWide) {
        fall = 1 - dd / bWide
        e = 1 - bAge[j] / bLife[j]
        b = b + fall * e * e * bGain
      }
    }
  }
  if (b > 0) {
    bk = min(b, 1)
    h = h + (bHue - h) * bk
    v = v + b
  }
  if (raw > thr) {
    t = (raw - thr) / (1 - thr)
    v = v + t * t * thrGain
    sa = sa * (1 - 0.6 * t)
  }
  v = v * env
  hsv(h, sa, v)
}
