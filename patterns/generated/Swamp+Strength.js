// Swamp+Strength -- GENERATED by tools/patterngen.py. Do not edit here.
// Swamp under the Strength aura.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
wfA = 2
wsA = 0.6
wfB = 3
wsB = 0.9
hA = 0.375
hB = 0.402
sA = 0.9225
sB = 0.9225
vA = 0.04
vB = 0.3
N = 4
bWide = 8
bGain = 0.42
bMin = 4
bVar = 4
bDrift = 7
bHue = 0.36
bSat = 0.8
bPos = array(N)
bAge = array(N)
bLife = array(N)
for (i = 0; i < N; i++) {
  bPos[i] = random(pathLen)
  bLife[i] = bMin + random(bVar)
  bAge[i] = random(bLife[i])
}
export function beforeRender(delta) {
  dt = delta / 1000
  tA = time(wsA)
  tB = time(wsB)
  et = time(0.03662)
  env = 0.35
  if (et < 0.12) env = 1 - (1 - 0.35) * (et / 0.12)
  if (et > 0.20 && et < 0.34) env = 0.35 + 0.455 * (1 - (et - 0.20) / 0.14)
  for (i = 0; i < N; i++) {
    bAge[i] = bAge[i] + dt
    bPos[i] = bPos[i] + bDrift * dt
    if (bPos[i] >= pathLen) bPos[i] = bPos[i] - pathLen
    if (bAge[i] >= bLife[i]) {
      bAge[i] = 0
      bLife[i] = bMin + random(bVar)
      bPos[i] = random(pathLen)
    }
  }
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = wave(u * wfA + tA) * 0.6 + wave(u * wfB - tB) * 0.4
  f = raw * raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  b = 0
  for (j = 0; j < N; j++) {
    dd = p - bPos[j]
    dd = dd - pathLen * floor(dd / pathLen + 0.5)
    dd = abs(dd)
    if (dd < bWide) {
      fall = 1 - dd / bWide
      e = triangle(bAge[j] / bLife[j])
      b = b + fall * fall * e * e * bGain
    }
  }
  if (b > 0) {
    bk = min(b, 1)
    h = h + (bHue - h) * bk
    sa = sa + (bSat - sa) * bk
    v = v + b
  }
  v = v * env
  hsv(h, sa, v)
}
