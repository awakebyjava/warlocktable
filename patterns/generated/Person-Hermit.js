// Person-Hermit -- GENERATED by tools/patterngen.py. Do not edit here.
// A single lantern brightens at one spot and holds.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
cueEl = 0
fWide = 50
fAt = 0.5
hA = 0.1
hB = 0.1
sA = 0.7
sB = 0.7
vA = 0
vB = 0.65
function fixedField(pp) {
  dd = pp - fAt * pathLen
  dd = dd - pathLen * floor(dd / pathLen + 0.5)
  dd = abs(dd)
  if (dd >= fWide) return 0
  fl = 1 - dd / fWide
  return fl * fl
}
export function beforeRender(delta) {
  dt = delta / 1000
  cueEl = cueEl + dt
  env = 0.05 + 0.95 * min(cueEl / 3.24, 1)
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = fixedField(p)
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  v = v * env
  hsv(h, sa, v)
}
