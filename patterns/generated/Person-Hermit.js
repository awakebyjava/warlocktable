// Person-Hermit -- GENERATED by tools/patterngen.py. Do not edit here.
// A lantern carried slowly round the table.
segStart = [ 60, 502,  0, 705, 180, 240, 120, 443]
segLen   = [ 60, 203, 60,  59,  60, 203,  60,  59]
pathPos = array(pixelCount)
for (i = 0; i < pixelCount; i++) pathPos[i] = -1
pathLen = 0
for (s = 0; s < 8; s++) {
  for (k = 0; k < segLen[s]; k++) {
    idx = segStart[s] + k
    if (idx < pixelCount) { pathPos[idx] = pathLen; pathLen = pathLen + 1 }
  }
}
cueEl = 0
gate = 0
cWide = 30
cN = 1
cTail = 4
ct = 0
hA = 0.1
hB = 0.1
sA = 0.72
sB = 0.72
vA = 0
vB = 0.68
N = 4
bWide = 7
bGain = 0.3
bMin = 0.6
bVar = 1.4
bRest = 0.8
bDrift = 3
bHue = 0.09
bPos = array(N)
bAge = array(N)
bLife = array(N)
bAmp = array(N)
for (i = 0; i < N; i++) {
  bPos[i] = random(pathLen)
  bLife[i] = bMin + random(bVar)
  bAge[i] = random(bLife[i])
}
function cometField(pp) {
  acc = 0
  for (ci = 0; ci < cN; ci++) {
    hp = (ct + ci / cN) * pathLen
    dd = pp - hp
    dd = dd - pathLen * floor(dd / pathLen + 0.5)
    wide = cWide
    if (dd < 0) wide = cWide * cTail
    dd = abs(dd)
    if (dd < wide) {
      fl = 1 - dd / wide
      acc = acc + fl * fl
    }
  }
  return min(acc, 1)
}
export function beforeRender(delta) {
  dt = delta / 1000
  cueEl = cueEl + dt
  ct = (cueEl / 9) * 0.55
  gate = min(cueEl / 0.9, 1)
  gOut = (9 - cueEl) / 1.6
  if (gOut < 0) gOut = 0
  if (gOut > 1) gOut = 1
  gate = gate * gOut
  for (i = 0; i < N; i++) {
    bAge[i] = bAge[i] + dt
    bPos[i] = bPos[i] + bDrift * dt
    if (bPos[i] >= pathLen) bPos[i] = bPos[i] - pathLen
    if (bAge[i] >= bLife[i]) {
      bAge[i] = -random(bRest)
      bLife[i] = bMin + random(bVar)
      bPos[i] = random(pathLen)
    }
    bAmp[i] = 0
    if (bAge[i] >= 0) {
      e = triangle(bAge[i] / bLife[i])
      bAmp[i] = e * e * bGain
    }
  }
}
export function render(index) {
  p = pathPos[index]
  if (p < 0) { rgb(0, 0, 0); return }
  u = p / pathLen
  raw = cometField(p)
  f = raw
  h = hA + (hB - hA) * (f)
  sa = sA + (sB - sA) * (f)
  v = vA + (vB - vA) * (f)
  b = 0
  for (j = 0; j < N; j++) {
    if (bAmp[j] > 0) {
      dd = p - bPos[j]
      dd = dd - pathLen * floor(dd / pathLen + 0.5)
      dd = abs(dd)
      if (dd < bWide) {
        fall = 1 - dd / bWide
        b = b + fall * fall * bAmp[j]
      }
    }
  }
  if (b > 0) {
    bk = min(b, 1)
    h = h + (bHue - h) * bk
    v = v + b
  }
  v = v * gate
  hsv(h, sa, v)
}
